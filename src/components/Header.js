import React from "react";

const Header = () => {
  return (
    <div
      style={{
        backgroundColor: "gray",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <h3>Weathercast</h3>
    </div>
  );
};

export default Header;
